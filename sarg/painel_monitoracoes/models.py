# -*- coding: utf-8 -*-
from django.db import models

# Create your models here.
#class PROJETOS(models.Model):
   # logo = models.ImageField(upload_to='media',  blank=True, null=False)
   # cliente = models.CharField(u'Cliente', max_length = 75, blank=True, null=False) 
   # projeto = models.CharField(u'Projetos', max_length = 75, blank=True, null=False)
 #   def __str__(self):
#         return '%s - %s' % (self.cliente, self.projeto)
#    class Meta:
#        unique_together = ('cliente', 'projeto')
